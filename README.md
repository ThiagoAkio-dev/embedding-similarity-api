# embedding-similarity-api

A Python project that generates sentence embeddings and calculates semantic similarity between user input and reference documents. Planned to evolve into a FastAPI-based semantic similarity API.